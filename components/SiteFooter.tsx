export default function SiteFooter() {
  return (
    <footer className="w-full border-t border-foreground/10">
      <p className="mx-auto max-w-3xl px-4 py-4 text-xs text-muted text-center font-mono">
        Variantle — guess the chess opening in 6 tries.
      </p>
    </footer>
  );
}
